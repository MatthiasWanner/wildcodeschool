function camelCase(string){
    array = string.split("");
    for(let i=O; i<array.length; i++){
        if(array[i] === " " || array[i] === "-" || array[i] === "_" ||){
            
        }
    }
}